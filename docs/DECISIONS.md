# 决策记录（ADR）

> **这个文件怎么用**：只记录**已经拍板**的决策和当时的理由。
> 规则是**只追加，不修改旧条目**。要推翻某个决定，就新增一条 ADR 并标注
> "取代 ADR-XXXX"，而不是回头改历史。这样三个月后还能看懂当初为什么这么选。

---

## ADR-0001 项目独立建仓，路径 `~/projects/homecheck`

- **状态**：已接受
- **日期**：2026-09-13
- **背景**：需要一个能跑通完整协作流程的练手项目。当前可用位置有两个问题：
  DSH 工作区（`<工作区路径>`）是 DeepSeek Harness 自身的代码库；
  而 home 目录本身也是一个 git 仓库，只有 1 个提交、大量文件处于未跟踪状态。
- **决定**：homecheck 独立建仓，路径 `~/projects/homecheck`，与上述两个仓库都无关。
- **理由**：练手项目不应污染任何现有仓库；独立仓库可以随时整体回滚甚至直接删掉。
- **后果**：
  - ✅ 隔离干净，搞砸了删目录即可，不影响任何现有工作
  - ⚠️ 需要单独初始化 git（**尚未执行**，等待明确批准）
  - ⚠️ 该路径在当前 DSH 文件沙箱之外，每次写入需要单独授权

---

## ADR-0002 技术栈：Python 3 + 标准库优先

- **状态**：已接受
- **日期**：2026-09-13
- **背景**：需要一个直接在本地文件系统上工作的命令行工具。
- **决定**：使用 Python 3.12（`python3`）。
  **标准库优先**：`pathlib` / `os` / `hashlib` / `argparse` / `unittest`。
  运行时零第三方依赖。
- **理由**：
  - 本机现成可跑，不需要安装任何东西
  - 标准库完整覆盖扫描、哈希、CLI 解析、测试这些需求
  - 依赖越少，长期可维护性越好，也避开供应链风险
- **后果**：
  - ✅ `git clone` 下来就能跑，没有环境搭建步骤
  - ✅ 测试用标准库 `unittest`，不需要 pytest
  - ⚠️ 进度条、彩色输出这类体验功能要自己写，或者干脆放弃
- **已否决的备选**：
  - Node / TypeScript —— 本机虽有 node 环境，但做文件系统工具标准库不如 Python 顺手
  - `click` / `rich` 等第三方库 —— 当前规模用不上，需要时再议

---

## ADR-0003 文档先行：M0 定稿前不写代码

- **状态**：已接受
- **日期**：2026-09-13
- **背景**：本项目同时是协作流程的训练场。主要风险是**方向漂移**和**范围蔓延**，
  而不是编码难度。
- **决定**：`PRODUCT.md` 第 8 节的开放问题（Q1–Q7）全部确认之前，不写任何功能代码。
- **理由**：一旦开始写代码，尚未确定的边界会被实现细节反向锁死，那时再改成本高得多。
- **后果**：前期看起来慢，但方向的最终控制权始终在项目所有者手里。

---

## ADR-0004 默认只读；写操作需要三重授权

- **状态**：已接受（2026-09-13）
- **日期**：2026-09-13
- **背景**：这是整个项目风险最高的部分 —— 一个能删除用户文件的工具。
- **决定**：工具的默认行为是**零写入**。任何改动文件系统的操作，
  必须同时满足三个条件才允许发生：
  1. 显式传入 `--apply`
  2. 逐个动作人工确认
  3. 执行前生成备份清单（记录原路径与去向）
- **理由**：把"误伤"从"可能发生"变成"必须三次主动同意才会发生"。
- **后果**：
  - ✅ 默认使用绝对安全
  - ⚠️ 批量清理会变得啰嗦 —— 这是**故意**的

---

## ADR-0005 非目标 N1–N6 是硬边界

- **状态**：已接受（2026-09-13）
- **日期**：2026-09-13
- **决定**：`PRODUCT.md` 第 4 节的 N1–N6 视为硬边界，不是"以后再说"。
  任何一条要突破，必须新增 ADR 并说明理由。
- **理由**：大项目失控通常不是因为做错了什么，而是因为什么都做了一点。
- **后果**：功能请求只要落在 N1–N6 内，默认答案是"不做"，需要额外理由才能推翻。

---

## ADR-0006 Q1–Q7 全部确认

- **状态**：已接受
- **日期**：2026-09-13
- **背景**：`PRODUCT.md` 第 8 节遗留 7 个开放问题，按 ADR-0003 构成开工前置条件。
- **决定**：7 项全部采纳 AI 提出的倾向值：

| 编号 | 问题 | 决定 |
|---|---|---|
| Q1 | 默认扫描根 | **当前目录**；扫 home 需显式传参 |
| Q2 | "陈旧"的阈值 | 180 天，可配置 |
| Q3 | "大文件"的阈值 | 100 MB，可配置 |
| Q4 | 符号链接 / 空文件 | 不跟随符号链接；空文件**不**算重复 |
| Q5 | 清理方式 | **v1 只输出命令，工具自身不执行删除** |
| Q6 | 权限拒绝时 | 跳过并记录，结尾汇总 |
| Q7 | 性能目标 | 待 S1 基线数据后确定 |

- **后果**：ADR-0003 的"不写代码"前置条件已满足，S1 可以开工。
  Q7 仍未定量，S1 完成后需补测并回填本条。

## ADR-0007 S2–S4 的实现决策集中在 SPEC 文档中

- **状态**：已接受（2026-09-13）
- **背景**：所有者要求一次完成 S2–S4。逐项确认会阻塞进度，但完全由 AI 拍板
  又违背本项目"边界由所有者定"的原则。
- **决定**：把 S2–S4 所有需要决策的点（D1–D14）显式列进
  `docs/SPEC-S2-S4.md` 第 0 节，实现采用"AI 倾向值"，**决策待所有者事后否决**。
- **理由**：让决策可见、可逐条回滚，而不是埋在代码里。
- **后果**：所有者复核该表即可否决任意一条；每项都不影响其他项的独立性。

## ADR-0008 报告只输出到 stdout，不写文件（S3）

- **状态**：已接受（2026-09-13）
- **背景**：`--json` / `--markdown` 需要一个去向。写成文件更方便，但会
  让"默认零写入"的承诺出现例外。
- **决定**：`--json` 与 `--markdown` **只写 stdout**，不提供输出文件选项。
  落盘由用户自行重定向：`homecheck . --json > report.json`。
- **理由**：CONVENTIONS §6 规定 `apply.py` 之外禁止写文件（含临时文件）。
  保持这条不变量，比多一个便利选项更有价值。
- **后果**：
  - ✅ "不传 `--apply` 就零写入"这条承诺至今没有任何例外
  - ⚠️ 用户要记一次 shell 重定向

## ADR-0009 建议输出量必须封顶（实跑后追加）

- **状态**：已接受（2026-09-13）
- **背景**：首次实跑 `~/code` 发现 **2000+ 组重复文件**（`node_modules` 噪音），
  建议引擎逐组输出，报告被彻底刷屏，不可用。
- **决定**：
  1. 建议里最多为 **5 组**重复文件附删除命令，其余合并为一条摘要（D13）
  2. 新增 `--min-duplicate-bytes`（默认 1），由用户自行抬高查重门槛（D14）
  3. **不**擅自修改默认门槛 —— Q4 只说了排除空文件
- **理由**：工具在真实数据上不可用，等同于没做。但改变默认语义属于产品决策，
  必须留给所有者。
- **后果**：`~/code` 的报告从数千行降到 60 行。默认行为仍保留全量查重。

## ADR-0010 S5 遵循 Q5：只生成执行计划，不执行改动

- **状态**：已接受（2026-09-13）
- **背景**：`PRODUCT.md` 内部存在冲突 ——
  - **G7**：显式 `--apply` + 逐条确认 + 备份清单，三者齐备才可能移动/删除
  - **Q5**：v1 只输出命令，工具自身不执行删除
- **决定**：S5 遵循 **Q5**，即只产出可复核的「执行计划」，不执行任何动作。
  G7 中"真正执行"的部分**未实现**，留待所有者明确授权后另开切片。
- **理由**：
  1. Q5 是针对"清理方式"专门拍的板，比 G7 的概述更具体
  2. Q5 确认时间更晚
  3. 不执行的工具无法销毁数据 —— 冲突时向安全侧倾斜
- **后果**：
  - ✅ 工具至今没有任何能改动文件系统的代码路径（`apply.py` 也只读）
  - ⚠️ G7 的承诺目前**只兑现了一半**：有计划与审计清单，没有执行
  - ⚠️ 若所有者要真正的 `--apply`，需新增 ADR 覆盖本条

---

## ADR-0011 S6 补齐测试到 102 个，含 CLI 端到端

- **状态**：已接受（2026-09-13）
- **背景**：S1–S5 的测试集中在模块层，CLI 的参数解析、退出码、
  stdout 纯度只有人工验证过。
- **决定**：新增 `tests/test_cli.py`（端到端调用 `main()`），并补齐边界用例：
  符号链接成环、含空格与 emoji 的文件名、60 层深目录、扫描后文件消失、
  体积单位边界、建议输出封顶。
- **理由**：真实使用走的是 CLI，模块层全绿不代表命令行行为正确。
- **后果**：
  - ✅ 测试 66 → 102
  - ✅ 退出码 0/1/2 与 `--json` 的 stdout 纯度被自动化锁住
  - ✅ 新增一条硬断言：`--plan` 之后目标文件**仍然存在**

## ADR-0012 S7：部分覆盖 Q5，允许"可恢复的执行"

- **状态**：已接受（2026-09-13，由所有者指示"执行"）
- **背景**：所有者要求实现真正的 `--apply`，即覆盖 Q5 的
  「v1 只输出命令，工具自身不执行删除」。
- **决定**：**部分**覆盖，边界如下：
  - ✅ 新增能力：按计划把冗余副本**移入隔离目录**（可恢复）
  - ❌ **不新增**能力：不可逆删除。工具至今没有任何 `rm` / `unlink` 调用
- **理由**：
  1. `PRODUCT.md` 的 G7 原文是"移动/删除"，实现"移动"这一半即已兑现承诺
  2. 移动可逆 —— 隔离目录保留相对路径，已实际演练可原样移回
  3. 所有者的原话是"执行"，而"移动"就是执行；"删除"是更强的主张，
     不该由一次模糊指示默认获得
- **后果**：
  - ✅ 工具仍然**没有任何能销毁数据的代码路径**
  - ⚠️ 想要不可逆删除，需要再一条明确指示 + 新的 ADR
  - ⚠️ README 与 SPEC-S5 里"本工具不会执行任何动作"的措辞已更新为
    "不会删除，但可以移入隔离目录"

---

## ADR-0013 Q7 性能基线：扫描阶段达标，查重不设时限

- **状态**：已接受（2026-09-13）—— 本条**回答 Q7**
- **背景**：Q7 要求给出性能目标。此前一次"完整 home + 全量查重"的任务
  运行 40 分钟未结束并被主动终止，当时**猜测**是内存/swap 问题。
  后续补测推翻了该猜测。
- **实测数据**（macOS / miniforge Python 3.12）：

  | 场景 | 规模 | 耗时 |
  |---|---|---|
  | `~/Github`，限深 3 层，**含全量查重** | 25,507 文件 / 1.52 GB | **0.26 s** |
  | 完整 home，**仅扫描**（`--skip-duplicates`） | **3,360,177 文件 / 302,008 目录 / 352.03 GB** | **114.09 s** |
  | 完整 home，含全量查重 | 同上 | **40 分钟未完成**，终止 |

  同一次完整扫描的其它观测：跳过符号链接 45,928 个；149 个目录因权限未读取；
  `Library/` 独占 250.63 GB（71.2%）；≥100MB 的文件 284 个（191.82 GB），
  其中 212 个超过 180 天未修改（161.9 GB）。

- **归因（修正先前的猜测）**：
  查重的耗时与**待哈希的数据量**成正比。home 有 352 GB、336 万个文件，
  其中大量文件大小相同因而会被完整读取。**这是 I/O 密集，不是缺陷、也不是卡死。**
  内存不是主因：336 万 × 356 字节 ≈ 1.2 GB，可接受；
  实测 `slots=True` 仅省 9%（`Path` 对象占大头），**因此未采纳**。
- **决定**：
  1. **Q7 的目标针对"扫描阶段"**：350 GB / 336 万文件 ≤ **3 分钟**（实测 114 s，约 1.6 倍余量）
  2. **查重阶段不设时限** —— 它的耗时由数据量决定，不由实现质量决定，
     硬定一个数字会变成误导
  3. **不**擅自抬高 `--min-duplicate-bytes` 默认值（那会改变 Q4 语义）
- **后果**：
  - ✅ Q7 关闭：扫描阶段有可验收的目标与实测基线
  - ⚠️ 在 352 GB 的 home 上跑全量查重就是会很慢，用户需自己判断值不值
  - ✅ 已知降级手段：`--skip-duplicates`、`--min-duplicate-bytes`、`--max-depth`
  - 📋 若将来要支持超大目录的查重，方向是"分桶落盘 / 流式处理"，
    而不是继续往内存里堆

---

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-13 | 初稿：ADR-0001 ~ ADR-0003 已接受，ADR-0004/0005 提议中 |
| v0.2 | 2026-09-13 | Q1–Q7 确认：新增 ADR-0006，ADR-0004/0005 转为已接受，移除待决表 |
| v0.3 | 2026-09-13 | 新增 ADR-0007/0008/0009：S2–S4 决策清单、报告不写文件、建议输出封顶 |
| v0.4 | 2026-09-13 | 新增 ADR-0010/0011：S5 遵循 Q5 只出计划、S6 测试补齐到 102 个 |
| v0.5 | 2026-09-13 | 新增 ADR-0012/0013：S7 部分覆盖 Q5（可移动不可删除）、全量查重的规模限制 |
