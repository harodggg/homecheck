# 待办与进度

最后更新：2026-09-13

## 已完成

- [x] **M0 立项**：`PRODUCT.md` / `DECISIONS.md` / `CONVENTIONS.md` / `SPEC-S1.md`
- [x] **Q1–Q7 全部确认**（见 `DECISIONS.md` ADR-0006）
- [x] git 仓库初始化
- [x] **S1** 只读扫描 + 体积统计（`SPEC-S1.md`）
- [x] **S2** 重复文件检测（`SPEC-S2-S4.md`）
- [x] **S3** 报告导出 JSON + Markdown（`SPEC-S2-S4.md`）
- [x] **S4** 建议引擎（`SPEC-S2-S4.md`）
- [x] 实跑修正：建议输出量封顶 + `--min-duplicate-bytes`（ADR-0009）

## 待办

- [ ] **S5** 安全的 apply 流程（dry-run / 备份清单 / 逐条确认）
- [ ] **S6** 测试补齐与边界情况
- [ ] **Q7** 性能基线回填 `ADR-0006`

## 待所有者复核

S2–S4 的决策点（`SPEC-S2-S4.md` 第 0 节 D1–D14）由 AI 按倾向值实现，
**逐条可否决**。其中风险最高的三条：

| 编号 | 决策 | 风险 |
|---|---|---|
| D10 | 生成的删除命令用 `rm --`，不用 `-r`/`-f` | 中：仍是删除命令，需用户自行复核 |
| D13 | 建议最多为 5 组附命令 | 低：其余组仍可通过 `--json` 拿到完整清单 |
| D14 | `--min-duplicate-bytes` 默认 1（保持 Q4 语义） | 低：默认仍会报出海量小文件重复 |

## 已确认的决策摘要

| 编号 | 决策 |
|---|---|
| Q1 | 默认扫描当前目录，扫 home 需显式传参 |
| Q2 | 陈旧阈值 180 天，可配置 |
| Q3 | 大文件阈值 100 MB，可配置 |
| Q4 | 不跟随符号链接；空文件不算重复 |
| Q5 | **v1 只输出命令，工具自身不执行删除** |
| Q6 | 权限拒绝时跳过并记录，结尾汇总 |
| Q7 | 性能目标待 S1 基线数据后确定 |

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-09-13 | 建立进度表；S1 完成 |
| v0.2 | 2026-09-13 | S2–S4 完成；新增待复核清单；记录实跑修正 |
